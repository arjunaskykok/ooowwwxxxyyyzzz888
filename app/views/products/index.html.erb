<div class="container">
  <div class="content">
    <h1>Products</h1>
    <ul>
      <% @products.each do |product| %>
        <li><%= link_to product.title, reviews_path(product.id) %></li>
      <% end %>
    </ul>
  </div>
</div>
